name: Deploy Infrastructure

on:
  workflow_call:
    inputs:
      overlay:
        description: "Kustomize overlay (prod or test)"
        required: true
        type: string
      namespace:
        description: "Kubernetes namespace"
        required: true
        type: string
      apply-manifests:
        description: "Whether to run kubectl apply -k (only when k8s configs changed)"
        required: true
        type: boolean
    secrets:
      KUBE_CONFIG:
        description: "Base64-encoded kubeconfig YAML"
        required: true
      DB_USER:
        required: true
      DB_PASSWORD:
        required: true

jobs:
  deploy-infra:
    name: Deploy Infrastructure
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4

      - name: Configure kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG }}" | base64 -d > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Ensure namespace exists
        run: kubectl create namespace ${{ inputs.namespace }} --dry-run=client -o yaml | kubectl apply -f -

      - name: Create or update db-credentials Secret
        run: |
          kubectl create secret generic db-credentials \
            -n ${{ inputs.namespace }} \
            --from-literal=POSTGRES_USER="${{ secrets.DB_USER }}" \
            --from-literal=POSTGRES_PASSWORD="${{ secrets.DB_PASSWORD }}" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Save current deployment images
        if: inputs.apply-manifests
        run: |
          kubectl get deploy -n ${{ inputs.namespace }} \
            -o jsonpath='{range .items[*]}{.metadata.name}{" "}{range .spec.template.spec.containers[*]}{.name}{" "}{.image}{"\n"}{end}{end}' \
            > /tmp/images-before.txt || true

      - name: Apply manifests
        if: inputs.apply-manifests
        run: kubectl apply -k k8s/overlays/${{ inputs.overlay }} -n ${{ inputs.namespace }}

      - name: Restore deployment images
        if: inputs.apply-manifests
        run: |
          if [ -s /tmp/images-before.txt ]; then
            while read -r deploy container image; do
              [ -n "$deploy" ] && \
                kubectl set image "deployment/$deploy" "$container=$image" \
                  -n ${{ inputs.namespace }} || true
            done < /tmp/images-before.txt
          fi
