name: Pull Request Checks

on:
  pull_request:
    types: [ opened, synchronize, reopened ]
    branches: [ main ]

permissions:
  contents: read
  pull-requests: write

jobs:
  detect-changes:
    name: Detect Changed Services
    runs-on: ubuntu-latest
    outputs:
      analytics-api: ${{ steps.filter.outputs.analytics-api }}
      frontend: ${{ steps.filter.outputs.frontend }}
      data-sync-svc: ${{ steps.filter.outputs.data-sync-svc }}
      nginx: ${{ steps.filter.outputs.nginx }}
    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            analytics-api:
              - 'analytics-api/src/**'
              - 'analytics-api/tests/**'
              - 'analytics-api/pyproject.toml'
              - 'analytics-api/ruff.toml'
              - '.github/workflows/lint-python.yml'
              - '.github/workflows/test-python.yml'
              - '.github/workflows/pr-check.yml'
            frontend:
              - 'frontend/src/**'
              - 'frontend/package.json'
              - 'frontend/bun.lock'
              - 'frontend/vite.config.ts'
              - 'frontend/tsconfig*.json'
              - 'frontend/eslint.config.js'
              - 'frontend/.prettierrc.json'
              - '.github/workflows/lint-typescript.yml'
              - '.github/workflows/test-typescript.yml'
              - '.github/workflows/build-typescript.yml'
              - '.github/workflows/pr-check.yml'
            data-sync-svc:
              - 'data-sync-svc/app/**'
              - 'data-sync-svc/build-logic/**'
              - 'data-sync-svc/config/**'
              - 'data-sync-svc/db/**'
              - 'data-sync-svc/domain/**'
              - 'data-sync-svc/gradle/**'
              - 'data-sync-svc/jolpica/**'
              - 'data-sync-svc/scheduled/**'
              - 'data-sync-svc/use-case/**'
              - 'data-sync-svc/build.gradle.kts'
              - 'data-sync-svc/settings.gradle.kts'
              - 'data-sync-svc/gradlew'
              - 'data-sync-svc/gradlew.bat'
              - 'data-sync-svc/Dockerfile'
              - '.github/workflows/verify-kotlin.yml'
              - '.github/workflows/pr-check.yml'
            nginx:
              - 'nginx/**'
              - '.github/workflows/lint-nginx.yml'
              - '.github/workflows/pr-check.yml'

  analytics-api-lint:
    name: Analytics API - Lint
    needs: detect-changes
    if: needs.detect-changes.outputs.analytics-api == 'true'
    uses: ./.github/workflows/lint-python.yml
    with:
      working-directory: './analytics-api'

  analytics-api-test:
    name: Analytics API - Test
    needs: analytics-api-lint
    uses: ./.github/workflows/test-python.yml
    with:
      working-directory: './analytics-api'

  frontend-lint:
    name: Frontend - Lint
    needs: detect-changes
    if: needs.detect-changes.outputs.frontend == 'true'
    uses: ./.github/workflows/lint-typescript.yml
    with:
      working-directory: './frontend'

  frontend-test:
    name: Frontend - Test
    needs: frontend-lint
    uses: ./.github/workflows/test-typescript.yml
    with:
      working-directory: './frontend'

  frontend-build:
    name: Frontend - Build
    needs: frontend-test
    uses: ./.github/workflows/build-typescript.yml
    with:
      working-directory: './frontend'

  data-sync-verify:
    name: Data Sync - Verify
    needs: detect-changes
    if: needs.detect-changes.outputs.data-sync-svc == 'true'
    uses: ./.github/workflows/verify-kotlin.yml
    with:
      working-directory: './data-sync-svc'

  nginx-lint:
    name: Nginx - Lint
    needs: detect-changes
    if: needs.detect-changes.outputs.nginx == 'true'
    uses: ./.github/workflows/lint-nginx.yml
    with:
      working-directory: './nginx'

  pr-check-status:
    name: PR Status Summary
    runs-on: ubuntu-latest
    needs: [ detect-changes, analytics-api-test, frontend-build, data-sync-verify, nginx-lint ]
    if: always()
    steps:
      - name: Check results
        run: |
          FAILED=false

          if [[ "${{ needs.detect-changes.result }}" != "success" ]]; then
            echo "❌ Detect Changes: ${{ needs.detect-changes.result }}"
            FAILED=true
          fi

          check() {
            local service="$1"
            local changed="$2"
            local result="$3"
            if [[ "$changed" == "true" && "$result" != "success" ]]; then
              echo "❌ $service: $result"
              FAILED=true
            else
              echo "✅ $service: ${result:-skipped}"
            fi
          }

          check "Analytics API" "${{ needs.detect-changes.outputs.analytics-api }}" "${{ needs.analytics-api-test.result }}"
          check "Frontend"      "${{ needs.detect-changes.outputs.frontend }}"      "${{ needs.frontend-build.result }}"
          check "Data Sync"     "${{ needs.detect-changes.outputs.data-sync-svc }}" "${{ needs.data-sync-verify.result }}"
          check "Nginx"         "${{ needs.detect-changes.outputs.nginx }}"         "${{ needs.nginx-lint.result }}"

          if [[ "$FAILED" == "true" ]]; then
            echo "❌ Some checks failed"
            exit 1
          fi

          echo "✅ All checks passed"
